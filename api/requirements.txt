fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
langgraph==0.2.62
langgraph-checkpoint-postgres==2.0.13
# [binary] pulls a prebuilt libpq -- plain `psycopg` needs a system libpq install,
# which Windows dev machines usually don't have; without this, importing the
# Postgres checkpointer raises "no pq wrapper available" at runtime.
psycopg[binary]==3.3.4
supabase==2.11.0
asyncpg==0.30.0
httpx==0.28.1
python-dotenv==1.0.1
google-genai==1.3.0
groq==0.15.0
tavily-python==0.5.0
pyjwt==2.10.1
# ES256 verification (Supabase's per-session signing keys) needs this --
# previously only present as someone else's transitive dependency.
cryptography==50.0.0
pytest==8.3.4
pytest-asyncio==0.25.1
